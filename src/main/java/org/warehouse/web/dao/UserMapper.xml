<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN"   
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.warehouse.web.dao.UserMapper">

	<select id="queryUser" resultType="java.util.Map">
		select
			t.id,
			t.name,
			t.password,
			t.state
		from
			sys_user t
		where
			t.name = #{name}
		and
			t.password = #{password}
	</select>
	<insert id="addUser">
		insert into sys_user(name, password, state)
		values(#{name}, #{password}, 0)
	</insert>
</mapper>